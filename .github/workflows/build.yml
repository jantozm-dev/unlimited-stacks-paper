name: Build and Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Install dependencies
        working-directory: higher-stacks---paper-plugin
        run: bun install --frozen-lockfile

      - name: Build application
        working-directory: higher-stacks---paper-plugin
        run: bun run build

      - name: Package build as JAR
        run: |
          mkdir -p "$GITHUB_WORKSPACE/release"
          jar --create \
            --file "$GITHUB_WORKSPACE/release/higher-stacks-paper-plugin-${GITHUB_REF_NAME}.jar" \
            -C "$GITHUB_WORKSPACE/higher-stacks---paper-plugin/dist" .

      - name: List release folder
        run: ls -R "$GITHUB_WORKSPACE/release"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Higher Stacks Paper Plugin ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            ${{ github.workspace }}/release/*.jar
          fail_on_unmatched_files: true
          body: |
            Build generated automatically from tag `${{ github.ref_name }}`.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
